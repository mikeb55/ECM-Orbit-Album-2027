#!/bin/sh
while read local_ref local_sha remote_ref remote_sha
do
  case "$remote_ref" in
    refs/heads/main|refs/heads/master)
      echo "Blocked: do not push directly to $remote_ref. Push your machine branch and merge via PR."
      exit 1
      ;;
  esac
done
exit 0